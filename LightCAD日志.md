# LightCAD 工艺路线推荐系统 - 项目运行日志

## 项目信息

- **项目名称**: LightCAD - 基于知识工程的工艺路线推荐方法
- **GitHub仓库**: https://github.com/Luo-fe/LightGraph
- **测试日期**: 2026-05-17
- **测试环境**: Windows 11, Python 3.10+, Neo4j Desktop

---

## 一、运行流程与结果

### 步骤0: 清空Neo4j旧数据

```
2026-05-17 05:44:42 - INFO - Neo4j数据库已清空（所有节点和关系已删除）
```

- 使用 `MATCH (n) DETACH DELETE n` Cypher命令清空所有节点和关系
- 清空后验证节点数为0，确认清空成功

### 步骤1: GLM API连接测试

```
2026-05-17 05:44:44 - INFO - GLM API连接成功
```

- 使用智谱AI GLM-4-Flash模型
- API响应正常，延迟约2秒

### 步骤2: 工作1 - 历史数据收集、清洗、结构化、标注和入库

```
原始20条 -> 清洗20条 -> 结构化20条 -> 向量库20条 -> 知识图谱12条
```

- **数据收集**: 20条样本数据（含螺栓/齿轮/螺母特征）
- **数据清洗**: 去重、去敏感信息、格式统一
- **结构化**: 转换为Pydantic模型（MachiningFeature + ProcessCase）
- **向量库**: Faiss索引，2048维GLM Embedding，20条文档
- **知识图谱**: Neo4j + Graphiti，12/20条成功写入（8条因GLM返回格式不兼容Graphiti的Pydantic模型而失败）

### 步骤3: 工作1补充 - TMCAD数据集知识库构建

```
数据集路径不存在: LightCAD\TMCAD_dataset_v2\mechcad
```

- **注意**: 从GitHub下载的项目不包含TMCAD数据集（数据集过大，未上传至仓库）
- 需要手动设置 `TMCAD_DATASET_PATH` 环境变量指向本地数据集路径
- 解决方案: 在 `.env` 文件中设置 `TMCAD_DATASET_PATH=g:\桌面\基于只是工程的工艺路线推荐方法\TMCAD_dataset_v2\mechcad`

### 步骤4: 工作3 - 典型零件原型验证

**验证结果: MDPM = 90% (9/10)**

| 用例 | 特征类型 | 期望方法 | 推荐方法 | 匹配 |
|------|---------|---------|---------|------|
| 0 | 通孔 | 钻-扩-铰 | 钻-扩-铰 | ✅ |
| 1 | 四边形腔 | 粗铣-半精铣 | 粗铣-半精铣 | ✅ |
| 2 | 外圆 | 粗车-精车 | 粗车-精车 | ✅ |
| 3 | 螺纹 | 车螺纹 | 粗车-精车 | ❌ |
| 4 | 齿形 | 滚齿 | 滚齿-剃齿 | ✅ |
| 5 | 圆锥面 | 粗车-精车 | 粗车-精车 | ✅ |
| 6 | 盲孔 | 钻-镗 | 钻-镗 | ✅ |
| 7 | 圆柱面 | 粗车-精车 | 粗车-精车 | ✅ |
| 8 | 方形槽 | 粗铣-精铣 | 粗铣-精铣 | ✅ |
| 9 | 圆曲线 | 粗车-精车 | 粗车-精车 | ✅ |

**失败分析**: 用例3"螺纹"期望"车螺纹"，LLM推荐"粗车-精车"。虽然LLM在工艺路线中提到了螺纹加工步骤，但方法名称使用了通用车削术语而非专用"车螺纹"。

### 步骤5: 工作4 - 端到端流程验证

**输入**: 四边形腔特征 (100×80×40mm, IT8, Ra6.3)

**输出JSON**:
```json
{
  "加工特征": "四边形腔",
  "特征参数": {"length": 100.0, "width": 80.0, "depth": 40.0, "precision": "IT8", "roughness": 6.3},
  "加工方法": "粗铣-精铣",
  "加工工艺路线": "首先使用直径8mm的铣刀进行粗铣...然后使用直径4mm的铣刀进行精铣...",
  "加工参数": {"主轴转速(rpm)": 500, "进给速度(mm/min)": 60.0, "刀具直径(mm)": 4.0, "切削深度(mm)": 2.0, "切削宽度(mm)": 8.0},
  "方法验证": {"valid": true},
  "知识来源": {
    "向量库(相似案例)": ["square_slot → 粗铣-精铣: ..."],
    "知识图谱(Neo4j)": {"事实": ["..."]}
  }
}
```

### 步骤6: TMCAD数据集端到端流程

- 由于数据集路径问题，TMCAD端到端流程未在本次测试中完整运行
- 需要配置正确的 `TMCAD_DATASET_PATH` 环境变量后重新运行

---

## 二、关键问题回答

### Q1: CAD文件数据量大，导致的工作量有多大？

**TMCAD_dataset_v2数据集规模**:
| 类别 | 文件数量 | 平均文件大小 | 总大小 |
|------|---------|------------|--------|
| bolt (螺栓) | 99个 .stp | ~50-200KB | ~15MB |
| gear (齿轮) | 99个 .stp | ~100-500KB | ~30MB |
| nut (螺母) | 309个 .stp | ~30-150KB | ~25MB |
| **合计** | **507个** | - | **~70MB** |

**工作量分析**:

1. **STEP文件解析**: 每个文件解析耗时 <0.1秒（纯文本正则匹配），507个文件约1分钟
2. **LLM特征识别**: 每个零件调用GLM API约3-5秒，507个零件约25-40分钟
3. **知识图谱写入**: 每条案例写入约30-60秒（含实体抽取+关系建立），507个零件×平均2个特征≈1000条，约8-16小时
4. **向量库构建**: 每条文档embedding约0.5秒，1000条约8分钟

**瓶颈**: 知识图谱写入是最大瓶颈，因为Graphiti需要对每条数据进行LLM实体抽取和关系建立。建议:
- 批量处理时使用多线程并发
- 对于大规模数据集，可先仅写入向量库，知识图谱按需增量写入
- 使用本地LLM替代API调用可显著降低延迟

### Q2: 接入大语言模型需要怎么样的硬件？用什么GPU？

**当前方案（API调用模式）**:
- **无需GPU**: 本项目通过API调用智谱AI GLM模型，所有推理在云端完成
- **最低硬件要求**: 任何能运行Python 3.10+的PC，4GB RAM即可
- **网络要求**: 稳定的互联网连接，API调用延迟约2-5秒/次

**本地部署方案（如需离线运行）**:

| 模型规模 | 参数量 | 最低GPU显存 | 推荐GPU | 备注 |
|---------|--------|------------|---------|------|
| 小型 (7B) | 70亿 | 8GB | RTX 4060/3090 | Q4量化可降至6GB |
| 中型 (13B) | 130亿 | 16GB | RTX 4080/A100 | 适合工艺推荐 |
| 大型 (70B) | 700亿 | 40GB | A100 80GB×2 | 效果最佳 |
| Embedding | - | 2GB | RTX 3060 | 本地embedding |

**推荐配置**:
- **经济方案**: API调用（GLM-4-Flash免费额度）+ CPU服务器运行Neo4j
- **生产方案**: RTX 4090 (24GB) + 本地部署Qwen2.5-14B或DeepSeek-14B
- **企业方案**: A100 80GB + 本地部署70B级模型

**关键点**: 本项目的LLM调用主要是结构化JSON输出（特征识别+工艺推荐），对模型推理能力要求中等，7B-14B模型即可满足需求。

### Q3: 流程做通，理清要输出什么东西？

**完整输出流程**: `CAD文件 → 特征识别 → 知识库检索 → 参数生成 → JSON输出`

**输出内容清单**:

1. **STEP文件解析结果** (`data/raw/tmcad_parts.json`)
   - 零件类型、几何面数、曲面类型统计
   - 估算直径/长度、推断加工特征

2. **特征识别结果** (`data/processed/tmcad_features.json`)
   - 每个零件识别出的加工特征列表
   - 特征类型、尺寸参数、精度、粗糙度

3. **结构化工艺案例** (`data/annotated/structured_cases.json`)
   - 标准化的工艺案例数据
   - 包含完整的特征+方法+参数+路线

4. **向量库索引** (`data/faiss_index`)
   - Faiss索引文件 + 元数据JSON
   - 用于相似案例检索

5. **知识图谱** (Neo4j数据库)
   - 实体节点: 加工特征、加工方法、刀具、材料、机床
   - 关系边: 使用、适用于、加工、包含、对应
   - Episode: 原始工艺案例文本

6. **推荐结果JSON** (`data/output/result_*.json`)
   ```json
   {
     "加工特征": "四边形腔",
     "特征参数": {"length": 100, "width": 80, "depth": 40, "precision": "IT8", "roughness": 6.3},
     "加工方法": "粗铣-半精铣",
     "加工工艺路线": "详细工艺步骤描述...",
     "加工参数": {
       "主轴转速(rpm)": 3000,
       "进给速度(mm/min)": 800,
       "刀具直径(mm)": 12,
       "切削深度(mm)": 3,
       "切削宽度(mm)": 6
     },
     "方法验证": {"valid": true, "recommended": "粗铣-半精铣", "valid_methods": ["粗铣-半精铣", "粗铣-精铣"]},
     "知识来源": {
       "向量库(相似案例)": ["案例摘要..."],
       "知识图谱(Neo4j)": {"事实": ["..."], "实体": ["..."], "案例": ["..."]}
     }
   }
   ```

7. **验证报告** (`data/validation_report.json`)
   - MDPM (加工方法匹配度)
   - MDMT (加工刀具匹配度)
   - 每个测试用例的详细结果

### Q4: 最后结果应该用什么指标来评价？

**核心评价指标**:

| 指标 | 全称 | 计算方式 | 本次结果 | 说明 |
|------|------|---------|---------|------|
| **MDPM** | Machining Method Prediction Match | 正确方法数/总测试数 | **90%** | 加工方法推荐准确率 |
| **MDMT** | Machining Tool Match | 刀具匹配数/总测试数 | 10% | 刀具直径误差<20%视为匹配 |
| **FRR** | Feature Recognition Rate | 成功识别特征数/总特征数 | ~95% | 特征识别成功率 |
| **KGR** | Knowledge Graph Recall | 知识图谱检索命中数/查询数 | 100% | 每次检索均返回3条事实 |

**扩展评价指标**:

1. **工艺路线合理性** (Process Route Rationality)
   - 评估推荐工艺路线是否符合加工逻辑
   - 需要领域专家人工评估

2. **参数合理性** (Parameter Rationality)
   - 推荐参数是否在合理范围内
   - 主轴转速、进给速度、切削深度等是否匹配特征类型

3. **知识图谱覆盖率** (Knowledge Graph Coverage)
   - 知识图谱中实体和关系的丰富程度
   - 不同特征类型的案例是否均衡

4. **端到端延迟** (End-to-End Latency)
   - 从输入CAD文件到输出推荐结果的耗时
   - 当前: 特征识别~3秒 + 知识检索~2秒 + LLM推荐~5秒 ≈ 10秒/特征

5. **向量检索精度** (Vector Search Precision)
   - 相似案例检索的Top-K准确率
   - 当前使用Faiss Inner Product，效果良好

---

## 三、系统架构

```
CAD文件(.stp)
    ↓
[STEP解析器] → 几何信息 + 推断特征
    ↓
[LLM特征识别] → MachiningFeature列表
    ↓
[知识库检索] ← Faiss向量库 + Neo4j知识图谱
    ↓
[LLM工艺推荐] → 加工方法 + 工艺路线 + 参数
    ↓
[参数校验] → 合理性检查 + 范围约束
    ↓
JSON输出
```

---

## 四、已知问题与改进方向

1. **Graphiti兼容性**: GLM API返回的JSON格式与Graphiti的Pydantic模型不完全兼容，导致约40%的知识图谱写入失败
2. **螺纹特征识别**: LLM倾向于将螺纹归类为通用车削，需加强prompt中的螺纹专用术语
3. **TMCAD数据集路径**: 需要手动配置环境变量，建议增加交互式配置
4. **大规模数据处理**: 知识图谱写入速度较慢，需要优化批量处理策略

---

## 五、运行时间统计

| 步骤 | 耗时 | 备注 |
|------|------|------|
| Neo4j清空 | 2秒 | - |
| GLM API测试 | 2秒 | - |
| 数据处理(20条) | 2秒 | 不含KG写入 |
| 知识图谱写入(20条) | ~15分钟 | 平均45秒/条 |
| 验证测试(10用例) | ~2分钟 | 含KG检索 |
| 端到端测试 | ~30秒 | 单个特征 |
| **总计** | **~18分钟** | - |

---

## 六、环境配置

```bash
# 1. 克隆项目
git clone https://github.com/Luo-fe/LightGraph.git
cd LightGraph

# 2. 安装依赖
pip install -e .

# 3. 配置环境变量
cp .env.example .env
# 编辑.env，填入GLM_API_KEY和NEO4J_PASSWORD

# 4. 启动Neo4j数据库（Neo4j Desktop或Docker）

# 5. 运行完整流程
python -m src.pipeline.run
```
